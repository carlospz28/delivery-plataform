# === Servidor ===
server.port=${PORT:8080}

# === Base de datos ===
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:delivery_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:juanita1234}

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# === JWT ===
jwt.secret=${JWT_SECRET:mi_clave_super_secreta_para_firmar_tokens_delivery_2026_min_32_chars}
jwt.expiration=86400000

# === Correo (Mailtrap) ===
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=8409074b8b1005
spring.mail.password=2de6bbc94a09cf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
